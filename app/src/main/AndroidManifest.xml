<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.PawPal_FInal" >

        <activity
            android:name=".ui.login.SigninActivity"
            android:exported="true" />
        <activity
            android:name=".ui.activities.UserInformationActivity"
            android:exported="true" />
        <activity
            android:name=".ui.activities.SecuritySettingsActivity"
            android:exported="true" />
        <activity
            android:name=".ui.homepage.ProfilePageActivity"
            android:exported="true" />
        <activity
            android:name=".ui.homepage.SchedulePageActivity"
            android:exported="true" />
        <activity
            android:name=".ui.homepage.HomepageActivity"
            android:exported="true" />
        <activity
            android:name=".ui.homepage.HistoryLogActivity"
            android:exported="true"/>


        <activity
            android:name=".ui.MainActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>